import React, { useContext } from 'react'; import { WishlistContext } from '../context/WishlistContext'; import ProductGrid from '../components/ProductGrid'; export default function Wishlist(){ const { items } = useContext(WishlistContext); return (<div className='max-w-6xl mx-auto p-4'><h2 className='text-xl font-semibold mb-4'>Your Wishlist</h2>{items.length===0?<p className='text-gray-500'>No items in wishlist</p>:<ProductGrid list={items}/>}</div>); }